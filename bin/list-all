#!/usr/bin/env bash
set -e

release_path="https://api.github.com/repos/starship/starship/releases"
cmd="curl -s"
token="${GITHUB_API_TOKEN:-$GITHUB_TOKEN}"
if [ -n "$token" ]; then
  cmd="$cmd -H 'Authorization: token $token'"
fi
cmd="$cmd $release_path"

versions=$(eval $cmd | grep -o '"tag_name": "v[^"]*"' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | awk '{a[NR]=$0}END{for(i=NR;i>=1;i--)printf a[i]" "}')
echo "$versions"
